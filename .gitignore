# Hugo build output and caches
site/public/
site/resources/
.hugo_build.lock
.task/

# Note: site/data/catalog.json IS committed (generated by `task generate`)
# so the Docker image can build with Hugo alone.
